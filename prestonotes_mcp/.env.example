# Optional: export these in your shell for terminal-only scripts (e.g. `uv run scripts/granola-sync.py`).
# The MCP server does not load this file — use `.cursor/mcp.env` for Cursor.

# GDRIVE_BASE_PATH=
# MYNOTES_ROOT_FOLDER_ID=
# GCLOUD_ACCOUNT=
