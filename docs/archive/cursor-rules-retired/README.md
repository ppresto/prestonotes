# Cursor rules (retired)

Rule files in this tree are archived copies moved out of `.cursor/rules/` when superseded. Do not re-enable without a deliberate task.

## Inventory

- **`workflow.mdc`** — former always-on main-Agent subagent pipeline rule (archived so new sessions do not load it by default).
